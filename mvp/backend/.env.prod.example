# Production Environment Configuration
# Copy this file to .env and update values as needed

# Database Configuration
DATABASE_URL=sqlite:///./chatbot.db

# Security
# Generate a strong secret key: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# Server Configuration
HOST=127.0.0.1
PORT=8000
DEBUG=False

# CORS Origins - Production subdomain
CORS_ORIGINS=["https://chat.ldttechnology.in","https://chat-api.ldttechnology.in"]

# OpenAI API Configuration
OPENAI_API_BASE=https://api.openai.com/v1

# Environment (DEV, PROD, LOCAL)
APP_ENV=PROD

# API Base URL for widget code generation
# This is the public URL where the API is accessible
API_BASE_URL=https://chat-api.ldttechnology.in
